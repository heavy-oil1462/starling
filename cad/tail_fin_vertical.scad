// VERTICAL TAIL FIN — print ONE, FLAT on the bed: no supports, layer lines
// along the span. The root tab glues into the tail sleeve's top fin socket;
// the TE groove takes the rudder hinge strip. Geometry lives in tail.scad.

use <tail.scad>

$fn = 60;
fin_vertical_part();
